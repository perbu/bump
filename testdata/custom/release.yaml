apiVersion: apps/v1
kind: Deployment
metadata:
  name: myorg-app
  labels:
    app: myorg-app
version: 0.9.0
spec:
  template:
    spec:
      containers:
        - name: app
          image: myorg/app:v0.9.0
